Flask==2.2.5
Flask-Cors==3.0.10
requests==2.31.0
pytesseract==0.3.10
Pillow==12.1.1
pdf2image==1.16.3
python-dotenv==1.0.0
Werkzeug==2.2.3
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.5.2
bcrypt==4.0.1
Flask-Limiter==3.5.0
# Postgres driver - only used if DATABASE_URL points to a postgres:// URL.
# SQLite (the default) needs no extra driver, so this doesn't change local dev.
psycopg2-binary==2.9.10
groq==1.7.0
gunicorn==23.0.0
# Error monitoring — set SENTRY_DSN env var to enable; leave blank to disable.
sentry-sdk[flask]==2.20.0